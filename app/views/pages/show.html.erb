<h1><%= @page.title %></h1>

<p> <%= @page.body %>  </p>

<%= link_to 'Edit', edit_page_path(@page) %>

<%= link_to 'Delete', @page, method: :delete %>

<!-- This is the http require method such as get, post or delete -->